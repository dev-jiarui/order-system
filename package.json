{
  "name": "order-system",
  "version": "1.0.0",
  "description": "订单系统，支持MongoDB和华为云FunctionGraph部署",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "NODE_ENV=dev node index.js",
    "uat": "NODE_ENV=uat node index.js",
    "prod": "NODE_ENV=prod node index.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "init-db": "node scripts/init-db.js",
    "init-db:dev": "NODE_ENV=dev node scripts/init-db.js",
    "init-db:uat": "NODE_ENV=uat node scripts/init-db.js",
    "init-db:prod": "NODE_ENV=prod node scripts/init-db.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0"
  },
  "keywords": [
    "order",
    "mongodb",
    "functiongraph",
    "hilton"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0",
    "mongodb-memory-server": "^8.16.1",
    "supertest": "^6.3.4"
  }
}
